version: "3.8"

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app/
    working_dir: /app/
    ports:
      - 3000:3000
      - 8000:8000
    stdin_open: true
    tty: true
